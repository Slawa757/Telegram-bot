services:
  - type: web
    name: telegram-deepseek-bot
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python ai_bot.py"
    envVars:
      - key: TELEGRAM_BOT_TOKEN
        sync: false
      - key: DEEPSEEK_API_KEY
        sync: false
